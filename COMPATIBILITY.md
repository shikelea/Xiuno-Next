# 插件与主题兼容矩阵

本页记录 Xiuno Next 对第三方插件和主题的**实际运行快照**，用于帮助站点维护者判断测试顺序，
不是插件市场、下载目录、质量背书或永久兼容承诺。

## 如何解读

- “最低已验证核心版本”表示该版本已经跑通过，不代表更旧版本一定不兼容。
- 包版本来自测试样本的 `conf.json`。第三方包可能在不改变版本号的情况下重新打包；如果实际文件不同，必须重新验证。
- Xiuno Next 不托管、不分发这些包。请从原作者认可的来源取得，并自行核对许可证、完整性和安全性。
- 核心只修复可覆盖一类旧包的通用兼容问题；包内业务逻辑、私有模板和文案问题归第三方包维护者处理。
- 未列出的包不是“不兼容”，而是尚无足够的固定样本和动态证据。

## 验证环境与范围

本快照在 2026-09-02 至 2026-09-03 的 Xiuno Next 4.5.4 开发线上完成，包含：

- Windows：PHP 8.5、MySQL 8、Chromium；覆盖安装与启用、设置、主题与回复、插件功能及 390×844 移动端页面。
- Linux Docker：Ubuntu 24.04、项目 `php:8.2-fpm-alpine`、MySQL 8.0、Nginx Alpine；覆盖依赖拒绝、合法启停、设置页、主题页、回帖和一次卸载/重装恢复。
- 10 个包按依赖顺序组合启用；页面检查包含 HTTP 4xx/5xx、控制台错误、PHP 错误、资源加载、可见编码实体和页面级横向溢出。

这些证据是在 4.5.4 开发线的多个受控批次中累积的，不表示每个功能都在每次核心提交后重新运行，
也不构成生产环境的性能或可用性 SLA。

## 验证矩阵

| 包 ID | 类型 | 已测试版本 | 最低已验证核心版本 | 已验证功能 | 已知限制与归属 |
| --- | --- | ---: | ---: | --- | --- |
| `huux_notice` | 插件 | 1.9 | 4.5.4 | 安装、启用/停用、通知入口渲染，以及作为 `till_quick_at` 依赖时的顺序校验 | 未穷举所有通知触发场景；当前未确认核心兼容缺陷 |
| `haya_favorite` | 插件 | 1.6.1 | 4.5.4 | 设置页、收藏、启停，以及卸载/重装后的可恢复性 | 与 Stately 后台附属包组合时，其私有覆盖模板引用不存在的语言键并显示 `lang[...]`；属于第三方包间集成问题 |
| `haya_post_like` | 插件 | 2.5.10 | 4.5.4 | 安装、启用/停用、主题与回复点赞、页面计数刷新 | 当前已测主路径未确认核心兼容缺陷 |
| `till_post_replies` | 插件 | 1.0.0 | 4.5.4 | 楼中楼创建、渲染和刷新后持久状态；未出现 `Server Response Empty!` | 新回复偶尔短暂显示 `-1秒前`，属于包级时间文案问题 |
| `till_quick_at` | 插件 | 1.0.0 | 4.5.4 | 依赖检查、启停，以及发主题、快速回复和高级回复页面共存 | 包元数据声明为 Stately 专用，不能外推为其他主题兼容；该范围属于包自身定位 |
| `till_lightbox` | 插件 | 1.0 | 4.5.4 | 安装、启用/停用，以及含图片/附件的主题页资源与脚本共存 | 未穷举所有图片格式和触摸手势；当前未确认核心兼容缺陷 |
| `till_verified_member` | 插件 | 1.1.4 | 4.5.4 | 设置页、认证标识与旧图标回退渲染 | credits 文案存在包级问题；核心不改写第三方业务文案 |
| `fox_search` | 插件 | 1.1 | 4.5.4 | 搜索表单、模糊/精确搜索、结果页和分页路由；桌面与移动端通过 | Stately 私有覆盖中的表单选择器与脚本不一致；核心只提供通用裸查询 GET 路由保护，不保证其他私有覆盖 |
| `abs_theme_stately` | 前台主题 | 1.0.36 | 4.5.4 | 安装、设置、首页/版块/主题、发主题、快速/高级回复、搜索、运行时资源和 390×844 页面 | 部分窄屏页面头像/用户名与标题区域拥挤；冷缓存首次编译可能需数秒；Firefox 可报告主题脚本 unreachable-code 与字体 glyph 警告。均为主题或环境侧限制 |
| `abs_themeacp_stately` | 后台主题附属包 | 1.1.3 | 4.5.4 | 对 Stately 的依赖门禁、后台安装/启停、设置页、操作图标与资源加载 | 依赖 `abs_theme_stately`；其 `haya_favorite` 私有覆盖存在前述缺失语言键，属于第三方组合问题 |

## 已验证的核心兼容边界

上述通过结果依赖 Xiuno Next 4.5.4 的通用能力，包括 PHP 8 Hook/生命周期兼容、插件依赖与任务锁、
设置默认值归一化、HTML/CSRF 注入、Bootstrap 4 到 5 的前端兼容、旧裸查询 GET 路由保护、
生成缓存发布以及 Docker 中受限的运行时 CSS/JS 服务。这些实现不识别表中的包名，也不修改第三方包文件。

如发现问题，请同时提供 Xiuno Next 版本、包 ID 与版本、包来源、可复现步骤、PHP/MySQL 版本、
错误日志和是否使用自定义主题。只有能在未修改第三方包的隔离环境中复现，才能判断修复应落在核心兼容层还是包本体。
